{
    "name": "vircadia-desktop",
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "SET TAURI_MODE=dev && tauri dev",
        "build": "SET TAURI_MODE=build && tauri build",
        "make-icon": "tauri icon ../public/assets/vircadia-icon-256.png",
        "tauri": "tauri"
    },
    "dependencies": {
        "@tauri-apps/api": "^1.5.0"
    },
    "devDependencies": {
        "@tauri-apps/cli": "^1.5.0",
        "typescript": "^5.0.2"
    }
}
