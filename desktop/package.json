{
    "name": "vircadia-desktop",
    "version": "0.0.0",
    "type": "module",
    "scripts": {
        "dev": "SET VRCA_DESKTOP_MODE=dev && tauri dev",
        "build": "SET VRCA_DESKTOP_MODE=build && tauri build",
        "make-icon": "tauri icon ../public/assets/vircadia-icon-256.png",
        "tauri": "tauri"
    },
    "dependencies": {
        "@tauri-apps/api": "^1.5.0"
    },
    "devDependencies": {
        "@tauri-apps/cli": "^1.5.0",
        "typescript": "^5.0.2"
    }
}
