name: '🖥️ Desktop Build Test'
on:
  pull_request:
    branches: [ "master" ]

jobs:
  test-tauri:
    strategy:
      fail-fast: false
      matrix:
        platform: [macos-latest, ubuntu-20.04, windows-latest]

    runs-on: ${{ matrix.platform }}
    steps:
      - uses: actions/checkout@v3

      - name: Setup Node
        uses: actions/setup-node@v3
        with:
          node-version: 16

      - name: Install Rust
        uses: dtolnay/rust-toolchain@stable

      - name: Install Dependencies (Ubuntu Only)
        if: matrix.platform == 'ubuntu-20.04'
        run: |
          sudo apt-get update
          sudo apt-get install -y libgtk-3-dev libwebkit2gtk-4.0-dev libappindicator3-dev librsvg2-dev patchelf

      - name: Install Frontend Dependencies
        run: npm ci && npm run install-desktop

      - name: Build
        env:
          NODE_OPTIONS: "--max_old_space_size=4096"
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          TAURI_MODE: build
        run: cd desktop && npm run tauri build

      - name: Upload MacOS Build
        if: matrix.platform == 'macos-latest'
        uses: actions/upload-artifact@v3
        with:
          name: build-desktop-${{ matrix.platform }}-${{ github.event.number }}
          path: ${{ runner.workspace }}/desktop/src-tauri/target/release/bundle/macos/Vircadia.app
          if-no-files-found: error

      - name: Upload Linux Build
        if: matrix.platform == 'ubuntu-20.04'
        uses: actions/upload-artifact@v3
        with:
          name: build-desktop-${{ matrix.platform }}-${{ github.event.number }}
          path: ${{ runner.workspace }}/desktop/src-tauri/target/release/bundle/deb/vircadia_0.0.0_amd64.deb
          if-no-files-found: error

      - name: Upload Windows Build
        if: matrix.platform == 'windows-latest'
        uses: actions/upload-artifact@v3
        with:
          name: build-desktop-${{ matrix.platform }}-${{ github.event.number }}
          path: ${{ runner.workspace }}/desktop/src-tauri/target/release/bundle/msi/Vircadia_0.0.0_x64_en-US.msi
          if-no-files-found: error
